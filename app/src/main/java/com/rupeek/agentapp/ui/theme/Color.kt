package com.rupeek.agentapp.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Orange Theme
val Orange80 = Color(0xFFFFCC80) // Light Orange
val OrangeGrey80 = Color(0xFFFFB074) // Softened Orange (instead of greyish tone)
val Peach80 = Color(0xFFFFAB91) // Soft Peach

val lightGrey = Color(0xFFEAEAEA) // Neutral support

val Orange40 = Color(0xFFFF9800) // Primary Orange
val OrangeGrey40 = Color(0xFFFF8A50) // Brighter soft orange
val Peach40 = Color(0xFFFF7043) // Vibrant peach

val primaryLight = Color(0xFFFF9800) // Main Orange
val onPrimaryLight = Color(0xFFFFFFFF) // White for contrast

val lightGreen = Color(0xFFE6F9F0) // Success support
val lightRed = Color(0xFFFFEBEB) // Error support

val primaryContainerLight = Color(0xFFFFF3E0) // Soft background orange
val onPrimaryContainerLight = Color(0xFFBF360C) // Strong orange-red for contrast

val secondaryLight = Color(0xFFFFA726) // Warm secondary orange
val onSecondaryLight = Color(0xFFFFFFFF)

val secondaryContainerLight = Color(0xFFFFE0B2) // Light pastel orange
val onSecondaryContainerLight = Color(0xFFEF6C00) // Medium orange

val correctLight = Color(0xFF3BB54A) // Confirmation green

val tertiaryLight = Color(0xFFFF7043) // Accent orange-red
val onTertiaryLight = Color(0xFFFFFFFF)

val tertiaryContainerLight = Color(0xFFFFCCBC) // Soft peach
val onTertiaryContainerLight = Color(0xFFD84315) // Stronger orange

val errorLight = Color(0xFFD32F2F)
val onErrorLight = Color(0xFFFFFFFF)

val errorContainerLight = Color(0xFFFFCDD2)
val onErrorContainerLight = Color(0xFFC62828)

val backgroundLight = Color(0xFFFAFAFA) // Softer, near-white background
val onBackgroundLight = Color(0xFF5D2B00)// Deep orange for readable contrast

val surfaceLight = Color(0xFFFAFAFA) // Muted surface orange
val onSurfaceLight = Color(0xFF050505) // Brighter orange for content

val surfaceVariantLight = Color(0xFFFFE0B2) // Lighter variant
val onSurfaceVariantLight = Color(0xFF0A0A0A)

val outlineLight = Color(0xFFFFA726) // Subtle bright outline
val outlineVariantLight = Color(0xFFFFCC80) // Light outline

val scrimLight = Color(0x80000000) // 50% black scrim

val inverseSurfaceLight = Color(0xFFFF6F00) // Bold dark orange
val inverseOnSurfaceLight = Color(0xFFFFF3E0) // Light peach

val inversePrimaryLight = Color(0xFFFF8F00) // Bright inverse orange
